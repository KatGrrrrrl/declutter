# QA check — Sep 14, 2026 (identity core, phases 0–5)

Follow-up to [`QA-2026-09-09.md`](QA-2026-09-09.md). Everything below was run from the QA
session against production and the deployed site. Nothing in `src/` was edited.

**State checked:** `origin/main` = `c355ddd` (identity core phases 0–5, all committed and
pushed; the local `core/identity` branch is at the same commit). Migrations 0001–0017 applied
(17/17 match). Ten Edge Functions deployed; five redeployed today for phase 0. Live bundle
`56f0a26b…` carries phase 5 (verified by string contents).

## Sign-ins — working

| Check | Result |
|---|---|
| `tools/e2e-phase0-functions.mjs` (real password sign-ins as owner / admin / outsider, then every function from both sides of its rule) | **13/13 PASS**, cleaned up |
| `tools/e2e-core/run.mjs` (two devices: create home, capture, decide, invite, join, refused helper decision, delete that must not resurrect, offline capture across an account switch) | **28/28 PASS**, cleaned up |
| `tools/sql/authority-behaviour.sql` (RLS + triggers as owner / helper / decider / outsider, rolled back) | **24/24 PASS** |
| Live `/login`: password form, "email me a code", Google, create account | renders, no console errors |
| Live wrong password | friendly "don't match" message, one 400 from auth, no crash, form stays usable |
| Auth logs, last 24 h | every password login 200, logouts 204; the only 4xx are the scripts' deliberate negative cases (422 already-registered, 403 malformed token). One real refresh-token grant at 13:41 UTC, 200 |
| Function logs, last 24 h | no unexpected 5xx; `daily-digest` ran at 23:00 UTC → 200 |

Not exercised: OTP email delivery and Google OAuth round-trip (both send mail or need a
real Google account; the buttons and the request paths are present).

## Route collision (Sep 9 blocker A1) — fixed and live

`(child)` and `(parent)` are gone; one `(home)` tab group decides tabs from DB membership.
Reloading `/inventory` and `/account` on the live site and on the dev server now keeps the
decider tabs on desktop and mobile widths. Helpers typing `/decide` are redirected.

## Still open from Sep 9

- React #418 hydration error on every load once a store exists (2× on mobile). Known;
  product decision pending.
- Fictional executor "Rebecca" on Export/Legacy (A2); memorandum export is a stub (B1);
  "Cancel anytime" with no cancel path (B2); the accessibility items (E1–E9).
- Blockers A3–A6 (owner backup vs `created_by`, shared item array, offline reverts, blob
  photos) were the target of phases 2–4. The core test above proves the new paths
  (deletes don't resurrect, offline captures survive an account switch, refusals are
  visible); a fresh static pass on the new `src/lib` has not been done.

## Housekeeping

- Untracked `b.js` (3.4 MB bundle dump) sits at the repo root since 11:42 today. Not mine;
  it should not be committed.
- `STATUS.md` and `HANDOFF.md` still describe the Sep 9 state.
- Advisors: unchanged (three intentional SECURITY DEFINER RPCs; leaked-password
  protection off; one MFA option).
