# Copy to .env for local dev. EXPO_PUBLIC_* values are embedded in the client
# bundle and are public (anon key only — never a service-role key here).
EXPO_PUBLIC_SUPABASE_URL=https://YOUR-PROJECT.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=YOUR-ANON-KEY
