/**
 * Owner "All items" — everything in the house, browsable and searchable, with
 * multi-select bulk actions and the archived shelf. Shares one implementation
 * with the helper Inventory tab.
 */

import { InventoryView } from '@/components/inventory-view';

export default function AllItemsScreen() {
  return <InventoryView />;
}
