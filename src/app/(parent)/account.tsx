/**
 * The parent role's "Account" tab. It renders the shared Settings screen so Log
 * out (and the rest of the account controls) is reachable from the bottom tab
 * bar on every screen, not only via the header button on Decide. On desktop
 * this tab is hidden (see _layout) because the left rail's footer already
 * carries Account & settings + Log out.
 */
export { default } from '../settings';
